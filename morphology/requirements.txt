# จำเป็นเฉพาะฟังก์ชัน "อ่านตารางจากภาพ (OpenCV)" เท่านั้น
# ถ้าไม่ติดตั้ง โปรแกรมยังเปิดใช้งานส่วนอื่นได้ตามปกติ
opencv-python>=4.5
numpy>=1.20
pillow>=8.0

# ใช้เฉพาะตอนรัน test_all.py เพื่อตรวจสอบผลกับไลบรารีมาตรฐาน
scipy>=1.7
